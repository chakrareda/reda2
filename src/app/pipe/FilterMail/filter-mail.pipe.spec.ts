import { FilterMailPipe } from '../FilterMail/filter-mail.pipe';

describe('FilterMailPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMailPipe();
    expect(pipe).toBeTruthy();
  });
});
