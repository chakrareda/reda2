import { IdgroupePipe } from '../FilterIdGroupe/idgroupe.pipe';

describe('IdgroupePipe', () => {
  it('create an instance', () => {
    const pipe = new IdgroupePipe();
    expect(pipe).toBeTruthy();
  });
});
